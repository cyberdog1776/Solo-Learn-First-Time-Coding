#
N = int(input())            #how many times to iterate 
sum=0                       #starting with zero
for i in range(1,N+1):      #from 1 to num of iterations
    sum+=i                  #add 1 every time to sum until
print(sum)                      #reaching 6 iterations, then
                                #add 1 more
                             
